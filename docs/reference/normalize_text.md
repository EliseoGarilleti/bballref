# Normalize text by removing accents and special characters

Normalize text by removing accents and special characters

## Usage

``` r
normalize_text(text)
```

## Arguments

- text:

  Character string to normalize

## Value

Normalized character string
